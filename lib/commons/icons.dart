import 'package:flutter/material.dart';

class GalleryAppIcons {
  static const Icon ic_search_splash_screen = Icon(Icons.search, color: Colors.blueAccent, size: 50.0);
  static const Icon ic_search_default = Icon(Icons.search);
  static const Icon ic_close_default = Icon(Icons.close);
}

